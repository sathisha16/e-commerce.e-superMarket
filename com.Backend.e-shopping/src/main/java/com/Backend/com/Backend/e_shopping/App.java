package com.Backend.com.Backend.e_shopping;

import org.springframework.context.annotation.AnnotationConfigApplicationContext;

import com.DAO.ProductDao;
import com.config.DBConfig;
import com.model.Product;

/**
 * Hello world!
 *
 */
public class App {
	public static void main(String[] args) {
		/*
		 * AnnotationConfigApplicationContext context = new
		 * AnnotationConfigApplicationContext(DBConfig.class);
		 * 
		 * ProductDao dao = (ProductDao) context.getBean("productDaoBean"); Product obj
		 * = new Product(); obj.setProductName("DELL_5000");
		 * obj.setProductDescription("this is insprion_5000_series");
		 * obj.setPrice(100000); obj.setQuantity(3);
		 * 
		 * dao.insertUpdateProduct(obj);
		 */
	}
}
